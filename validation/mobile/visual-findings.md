# Mobile visual findings — targeted foundation pass

The first independent 375px render of the public home route showed that the headline wrapped at nearly every word and consumed too much of the initial viewport. The hero was adjusted to use a smaller mobile floor, the full content width, a 20px phone gutter, and a safe-area-aware top offset. A second capture taken before the splash timeout showed only the expected branded launch screen, confirming the dark splash background rather than validating final layout. A full 5-second capture confirmed the adjusted headline is readable, but it exposed an awkward one-word second line in the primary action. The phone action label was shortened to "Start the assessment" and the latest render confirmed it is a full-width, single-line, thumb-reachable primary control.

The 375px pricing render confirmed that the safe-area-aware top offset keeps the investment heading clear of the fixed navigation, the annual switch is visually thumb-sized, and the plan cards are centered in a single column. The local preview carries the platform’s non-live banner at the viewport bottom, so the lower card and button area needs a published or authenticated-device check before it can receive a complete visual pass.

The 375px Ground landing render confirmed a centered single-column entry, clear navigation clearance, readable hero measure, large state-selection rows, and a full-width primary practice action. The 375px Load-Bearing Survey entry also kept its long reading measure, heading, explanatory copy, and entry controls within the viewport without horizontal clipping. The preview environment’s non-live banner obscures lower content in both captures, so final lower-viewport / keyboard verification still needs a published-device pass.

The 375px Resource Library render maintained a clear hierarchy, readable paragraph measure, centered category controls, and no visible horizontal overflow above the preview banner. The 375px Enter the Ground practice render maintained clear header spacing, three evenly sized selection cards with readable labels, and a centered practice card without media voids. The preview banner again prevents reliable lower-content and keyboard-state inspection in the local capture.

A forced reduced-motion 375px home capture retained the complete hero hierarchy, readable copy, and primary action without depending on entrance animation for comprehension. The preview banner still blocks the lower viewport, so this validates the above-fold motion fallback only.
